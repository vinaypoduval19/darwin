import {createStyles} from '@mui/material'

const styles = () =>
  createStyles({
    container: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: '28px'
    }
  })

export default styles
