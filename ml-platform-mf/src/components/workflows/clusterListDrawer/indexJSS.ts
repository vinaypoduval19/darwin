import createStyles from '@mui/styles/createStyles'

const styles = () =>
  createStyles({
    container: {
      height: '100%',
      width: '477px',
      position: 'relative'
    }
  })

export default styles
