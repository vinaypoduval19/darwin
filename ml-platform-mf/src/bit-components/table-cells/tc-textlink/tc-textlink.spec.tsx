import {render} from '@testing-library/react'
import React from 'react'
import {TcTextlinkAlign} from './tc-textlink.composition'

describe('test for tc-textlink', () => {
  it('dummy test for tc-textlink cell', () => {
    render(<TcTextlinkAlign />)
  })
})
