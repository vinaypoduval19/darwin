import {render} from '@testing-library/react'
import React from 'react'
import {DataCell} from './tc-data.composition'

describe('test for data cell', () => {
  it('test for data cell', () => {
    render(<DataCell />)
  })
})
