import {render} from '@testing-library/react'
import React from 'react'
import {BasicTcIcon} from './tc-icon.composition'

describe('test for tc-icon', () => {
  it('test for tc-icon', () => {
    render(<BasicTcIcon />)
  })
})
