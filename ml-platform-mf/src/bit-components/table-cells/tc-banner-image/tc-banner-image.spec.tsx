import {render} from '@testing-library/react'
import React from 'react'
import {BasicTcBannerImage} from './tc-banner-image.composition'

describe('test for tc-banner-image', () => {
  it('dummy test for banner-image', () => {
    render(<BasicTcBannerImage />)
  })
})
