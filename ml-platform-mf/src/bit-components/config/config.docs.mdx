---
description: A design tokens config for web components.
---

import {globalTokens} from './config'
import {aliasTokens} from './config'

We can use these config to access the deisgn token

### Component usage

```
import {aliasTokens} from '@sporta-technology/d11-web-components.config'

.primaryButton{
   color: `${aliasTokens.cta_primary_background_color}
}
```
