mutation RetryInstallLibrary($input: IRetryInstallLibrary) {
  retryInstallLibrary(input: $input) {
    status
    message
    data {
      packages {
        status
        version
        name
        id
      }
      cluster_id
    }
  }
}