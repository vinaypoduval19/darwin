query GetWorkflowsMetaData($names: [String]) {
  getWorkflowsMetaData(names: $names) {
    status
    message
    data {
      trigger_rules {
        id
        label
        value
        description
      }
    }
  }
}